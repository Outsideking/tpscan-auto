environment:
  # PayPal
  - PAYPAL_ENV=sandbox            # หรือ live
  - PAYPAL_CLIENT_ID=...
  - PAYPAL_CLIENT_SECRET=...
  - PAYPAL_RETURN_URL=https://yourdomain/paypal/return
  - PAYPAL_CANCEL_URL=https://yourdomain/paypal/cancel

  # Opn/Omise
  - OPN_SECRET_KEY=skey_test_xxx  # หรือ OMISE_SECRET_KEY
  - OPN_RETURN_URL=https://yourdomain/opn/return

  # Frontend (serve as static - ใส่ public key ใน HTML)
  # <script>window.OMISE_PUBLIC_KEY="pkey_test_xxx";</script>
